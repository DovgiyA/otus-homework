---
- name: enable docker
  systemd:
    name: docker
    enabled: yes
    state: started

- name: restart rsyslog
  systemd:
    name: rsyslog
    state: restarted

- name: restart elk
  shell: docker compose restart
  args:
    chdir: "{{ elk_dir }}"
