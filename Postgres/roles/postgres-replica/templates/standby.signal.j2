# standby.signal
# This file indicates that the PostgreSQL server should start as a standby.
# In PostgreSQL >= 12, this file replaces the old recovery.conf mechanism.
# The actual replication parameters (primary_conninfo, primary_slot_name)
# are configured in postgresql.conf.
