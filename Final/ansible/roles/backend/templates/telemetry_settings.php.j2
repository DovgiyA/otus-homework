<?php
// Telemetry settings - Generated by Ansible

// Type of db: "mysql", "sqlite" or "postgresql"
$db_type = 'postgresql';

// Password to login to stats.php
$stats_password = '{{ librespeed_stats_password }}';

// If set to true, test IDs will be obfuscated
$enable_id_obfuscation = true;

// If set to true, IP addresses will be redacted
$redact_ip_addresses = false;

// Postgresql settings
$PostgreSql_username = '{{ pg_speedtest_user }}';
$PostgreSql_password = '{{ pg_speedtest_password }}';
$PostgreSql_hostname = '{{ db_primary_ip }}';
$PostgreSql_databasename = '{{ pg_speedtest_db }}';
